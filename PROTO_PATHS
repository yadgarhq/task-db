# What this module vendors from yadgarhq/proto (D70). A subset, not the whole
# set — buf closes the import graph itself, so listing yadgar/task/v1 also brings
# yadgar/common/v1. Well-known types are not emitted; protoc supplies them.
yadgar/task/v1
